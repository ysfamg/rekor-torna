# Nginx Configuration - /etc/nginx/sites-available/rekor-torna
# Bu dosyayı kopyalayıp düzenleyin

server {
    listen 80;
    server_name rekortorna.com www.rekortorna.com;
    
    # SSL için yönlendirme (certbot kurduktan sonra aktif edin)
    # return 301 https://$server_name$request_uri;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# SSL sonrası:
# server {
#     listen 443 ssl http2;
#     server_name rekortorna.com www.rekortorna.com;
#     
#     ssl_certificate /etc/letsencrypt/live/rekortorna.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/rekortorna.com/privkey.pem;
#     
#     location / {
#         proxy_pass http://localhost:3000;
#         ... (yukarıdaki proxy ayarları)
#     }
# }
